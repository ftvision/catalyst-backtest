[project]
name = "catalyst-graph-compiler"
version = "0.1.0"
description = "Graph compiler package for Catalyst backtesting"
requires-python = ">=3.12"
dependencies = [
  "catalyst-contracts",
]

[tool.uv.sources]
catalyst-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

