[project]
name = "catalyst-result-reporter"
version = "0.1.0"
description = "Result reporting package for Catalyst backtesting"
requires-python = ">=3.12"
dependencies = [
  "catalyst-contracts",
]

[tool.uv.sources]
catalyst-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

