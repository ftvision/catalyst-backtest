[project]
name = "catalyst-market-data"
version = "0.1.0"
description = "Market data package for Catalyst backtesting"
requires-python = ">=3.12"
dependencies = [
  "catalyst-contracts",
]

[tool.uv.sources]
catalyst-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

