[project]
name = "catalyst-contracts"
version = "0.1.0"
description = "Shared Python contract models for Catalyst backtesting"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

