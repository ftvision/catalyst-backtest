# Tests

Shared cross-language fixtures and golden tests live here.

- `fixtures/`: deterministic market data and graph inputs
- `golden/`: expected outputs for known scenarios
- `conformance/`: behavior checks that future implementations must satisfy

